import { styled } from '@lapo';
import * as SwitchPrimitive from '@radix-ui/react-switch';

const StyledRoot = styled(SwitchPrimitive.Root, {});

const StyledSwitch = styled(SwitchPrimitive.Switch, {});
